line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",  # line length
    "E402",  # module level import not at top of file (needed for sys.path & QT_QPA_PLATFORM)
    "W291",  # trailing whitespace
    "W293",  # blank line contains whitespace
]
